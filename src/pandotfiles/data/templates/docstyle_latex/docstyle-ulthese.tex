\titre{$title$}
\soustitre{$subtitle$}
\auteur{$for(author)$$if(author.name)$$author.name$$else$$author$$endif$$sep$, $endfor$}
\annee{$if(year)$ $year$ $else$ \the\year{} $endif$}

\programme{$program$}
\direction{$director$}

\usepackage{polyglossia}
\setdefaultlanguage{french}
\setotherlanguages{english}

\makeatletter
  \def\fps@figure{ tbp }
  \def\fps@table{ tbp }
\makeatother


\renewcommand{\maketitle}{

  \frontmatter                    % pages liminaires

  $if(depot)$
    \frontispice
  $else$
    \pagetitre
    \frontispice
  $endif$
  
  $if(resume)$
    \chapter*{Résumé}                      % ne pas numéroter
    \phantomsection\addcontentsline{toc}{chapter}{Résumé} % inclure dans TdM

      $resume$
  $endif$

  $if(ulabstract)$
    \chapter*{Abstract}                      % ne pas numéroter
    \phantomsection\addcontentsline{toc}{chapter}{Abstract} % inclure dans TdM

      % \ulabstract              % résumé anglais
    $ulabstract$
  $endif$

  $if(resume)$
    \cleardoublepage
  $else$
  $if(abstract)$
    \cleardoublepage
  $endif$
  $endif$

  \setcounter{tocdepth}{2}
  \tableofcontents                % production de la TdM
  \cleardoublepage

  \listoftables                   % production de la liste des tableaux
  \cleardoublepage

  \listoffigures                  % production de la liste des figures
  \cleardoublepage


  $if(dedicace)$
    \dedicace{$dedicace$}
    \cleardoublepage
  $endif$

  $if(epigraphe)$
    \epigraphe{$epigraphe$}
    \cleardoublepage
  $endif$

  $if(remerciements)$
    \chapter*{Remerciements}         % ne pas numéroter
    \phantomsection\addcontentsline{toc}{chapter}{Remerciements}
    $remerciements$
  $endif$

\mainmatter                     % corps du document

}
